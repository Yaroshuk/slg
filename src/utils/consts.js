const baseSize = 50;
const isoHeight = 15;
const startX = 0;
const startY = 50;
const objectKeys = {
    empty: '0',
    wall: '1',
    player: 'X' 
} 

export default {
    baseSize,
    isoHeight,
    startX,
    startY,
    objectKeys
}